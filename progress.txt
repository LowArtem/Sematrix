## Codebase Patterns
- Keep the required repository scaffold protected by a lightweight pytest that asserts the fixed top-level and backend layer paths exist.
- Keep the PRD integration branch name distinct from the epic branch namespace; Git cannot create a branch like `foo` once epic branches already use `foo/...`.
- Guard Docker Compose changes with both `docker compose config --services` and a real `docker compose up --build` smoke test against the backend health route and frontend root.
- For localhost-only Compose exposure, use long-form `ports` mappings with `host_ip: 127.0.0.1` and verify the rendered `docker compose config` output in tests.
- For bind-mounted Postgres data under `./data/postgres`, set `PGDATA` to a child directory so `initdb` can initialize cleanly against the fixed host path.
- For Ollama GPU passthrough in this repo, prefer `deploy.resources.reservations.devices` over a top-level `gpus` key because the current Compose validator rejects `services.<name>.gpus` during `docker compose config`.
- For automatic Ollama model availability, use a one-shot `ollama-init` service that shares `./data/ollama`, waits for the API, and gates model-dependent services with `condition: service_completed_successfully`.
- When Ollama model compatibility outruns usable Docker tags, pin the official `ollama/ollama` image by digest in Compose so `ollama` and `ollama-init` stay reproducible while still matching the required model set.
- Keep Docker build context lean with a repo-root `.dockerignore`, especially excluding `frontend/node_modules` and `frontend/dist` so local frontend artifacts do not leak into image builds.
- For database-foundation stories, verify `alembic upgrade head` against a freshly created scratch PostgreSQL database through `docker compose run --rm --no-deps backend ...` instead of trusting file-only migration scaffolding.
- When an Alembic migration manually creates a PostgreSQL enum before table creation, use `postgresql.ENUM(..., create_type=False)` on the table column to avoid duplicate `CREATE TYPE` attempts during `op.create_table(...)`.
- Create the scratch validation database explicitly inside the Compose Postgres service before running `alembic upgrade head`, since Alembic will not create the target PostgreSQL database for you.
- Wait for `pg_isready` inside the Compose Postgres container before scratch-database `psql` setup, because the container may report as started before PostgreSQL accepts connections.
- In SQLAlchemy declarative models, do not use `metadata` as a Python attribute name; map the database column with an alternate attribute such as `metadata_json = mapped_column("metadata", ...)`.
- For schema/index stories, validate the final PostgreSQL indexes from `pg_indexes` against a scratch migrated database instead of assuming SQLAlchemy/Alembic declarations rendered exactly as intended.
- Keep Celery tasks in dedicated `app/workers` modules registered via the Celery app, and keep host-side backend-foundation tests source-based when they only need to verify architectural wiring.
- Keep backend ENV parsing centralized in `app/core/config.py` and have FastAPI/Celery bootstrap code consume shared settings there instead of scattering raw `os.getenv(...)` calls across entrypoints.

## [2026-03-14 18:59:47 +0300] - EP-003 / US-016
Epic: Backend application skeleton, configuration, and API contracts
Epic Branch: ralph/sematrix-mvp/ep-003-backend-foundation
Story: Implement environment-driven configuration with all mandatory ENV names
Thread: CLI run
- What was implemented
  - Added a shared `app.core.config` settings module that reads every mandatory spec ENV name with validated int/bool parsing and compose-friendly defaults.
  - Wired FastAPI startup and Celery bootstrap to consume the shared settings entrypoint so backend configuration now stays centralized instead of using scattered `os.getenv(...)` calls.
  - Updated the backend container command to respect `API_HOST` and `API_PORT`, and added tests that guard both the declared ENV surface and runtime parsing behavior.
- Files changed
  - `AGENTS.md`
  - `backend/Dockerfile`
  - `backend/app/core/__init__.py`
  - `backend/app/core/config.py`
  - `backend/app/main.py`
  - `backend/app/workers/celery_app.py`
  - `backend/tests/test_backend_config.py`
  - `backend/tests/test_backend_foundation.py`
  - `prd.json`
  - `progress.txt`
- Checks run and results
  - `python -m pytest` ✅ (20 passed)
  - `docker compose build backend` ✅
  - `docker compose run --rm --no-deps backend python -c "from app.core import load_settings; ..."` ✅
  - `docker compose down` ✅
- PRD updates made
  - Marked `US-016` as passed and added a durable note about the shared settings loader, validated ENV parsing, and centralized API/Celery config wiring.
  - Re-evaluated `EP-003`; it remains `passes: false` because the unified error handling, public DTO, and structured logging stories are still open.
- Learnings for future iterations:
  - Patterns discovered
    - Keep backend ENV parsing centralized in `app/core/config.py` so API and worker bootstraps share one validated settings entrypoint.
  - Gotchas encountered
    - Loading a module via `importlib.util.module_from_spec(...)` in host-side tests needs `sys.modules[spec.name] = module` before `exec_module(...)` when the module defines dataclasses under Python 3.14.
  - Useful context
    - `backend/Dockerfile` now launches Uvicorn through `API_HOST`/`API_PORT`, while Celery still picks broker/result-backend values from the shared settings loader.
---

## [2026-03-14 18:54:30 +0300] - EP-003 / US-015
Epic: Backend application skeleton, configuration, and API contracts
Epic Branch: ralph/sematrix-mvp/ep-003-backend-foundation
Story: Create the FastAPI application skeleton using the required layered architecture
Thread: CLI run
- What was implemented
  - Replaced the inline FastAPI stub with a `create_app()` entrypoint that wires public and `/api` routers from `app/api`.
  - Added API dependency wiring plus a small domain/infra split for system status handling so route handlers stay HTTP-only.
  - Moved the Celery ping task into `app/workers/tasks.py` and registered it from `celery_app.py`, keeping worker entrypoints thin and domain-driven.
  - Added backend-foundation tests that guard the new layer boundaries and router/task wiring without requiring host-side FastAPI or Celery imports.
- Files changed
  - `AGENTS.md`
  - `backend/app/main.py`
  - `backend/app/api/dependencies.py`
  - `backend/app/api/router.py`
  - `backend/app/api/routes/__init__.py`
  - `backend/app/api/routes/system.py`
  - `backend/app/domain/system.py`
  - `backend/app/infra/system.py`
  - `backend/app/workers/celery_app.py`
  - `backend/app/workers/tasks.py`
  - `backend/tests/test_backend_foundation.py`
  - `prd.json`
  - `progress.txt`
- Checks run and results
  - `python -m pytest` ✅ (16 passed)
  - `docker compose build backend` ✅
  - `docker compose run --rm --no-deps backend python -c "from app.main import app; from app.workers.tasks import ping; print(app.title); print(ping())"` ✅
  - `docker compose down` ✅
- PRD updates made
  - Marked `US-015` as passed and added a durable note about the new app factory, router/dependency wiring, and worker task delegation pattern.
  - Re-evaluated `EP-003`; it remains `passes: false` because the configuration, unified error handling, DTO, and structured logging stories are still open.
- Learnings for future iterations:
  - Patterns discovered
    - Keep Celery tasks in separate worker modules registered through the Celery app so task definitions can stay thin and avoid drifting into configuration files.
  - Gotchas encountered
    - Host-side pytest in this repo cannot assume FastAPI/Celery dependencies are installed, so architecture-guard tests should stay source-based unless the check runs inside the backend container.
  - Useful context
    - The current API skeleton exposes `/` and `/api/health` through `app/api/routes/system.py`, while `backend/app/main.py` only owns app creation and router registration.
---

## [2026-03-14 18:39:23 +0300] - EP-002 / US-014
Epic: Database schema, pgvector, and migrations
Epic Branch: ralph/sematrix-mvp/ep-002-db-schema-pgvector
Story: Implement all required indices and generated search columns
Thread: CLI run
- What was implemented
  - Added the required SQLAlchemy index declarations for note listing on `(folder_id, updated_at, id)`, the GIN-backed `notes.search_tsv`, reverse lookup support on `note_tags` and `note_assets`, and current-version lookup support on both processing-result tables.
  - Expanded the existing Alembic head revision to create and drop those six required indexes while preserving the exact RU+EN STORED `search_tsv` expression already fixed in the schema.
  - Extended the schema guard tests so the repository now checks for the new ORM and migration index declarations alongside the earlier generated-column coverage.
- Files changed
  - `backend/app/infra/models.py`
  - `backend/alembic/versions/20260314_1800_initialize_alembic.py`
  - `backend/tests/test_database_migration_setup.py`
  - `prd.json`
  - `progress.txt`
- Checks run and results
  - `python -m pytest` ✅ (13 passed)
  - `docker compose build backend` ✅
  - `docker compose up -d postgres` ✅
  - `docker compose exec -T postgres ... pg_isready` ✅
  - `docker compose exec -T postgres ... DROP/CREATE DATABASE sematrix_indexes_check` ✅
  - `docker compose run --rm --no-deps -e DATABASE_URL=postgresql+psycopg://sematrix:sematrix@postgres:5432/sematrix_indexes_check backend alembic upgrade head` ✅
  - PostgreSQL `pg_indexes` inspection for the six required listing/search/relation/result indexes ✅
  - `docker compose down` ✅
- PRD updates made
  - Marked `US-014` as passed and added a durable note about the required SQLAlchemy/Alembic indexes plus scratch-Postgres validation.
  - Re-evaluated `EP-002`; it remains `passes: false` because the `note_links` schema story is still open.
- Learnings for future iterations:
  - Patterns discovered
    - Scratch-database validation for schema/index work should inspect `pg_indexes` directly when acceptance criteria name exact PostgreSQL indexes.
  - Gotchas encountered
    - The join-table composite primary keys already cover `note_id`-leading lookups, so the extra relation-table indexes are most useful on the reverse `(tag_id, note_id)` and `(asset_id, note_id)` access paths.
  - Useful context
    - The exact RU+EN generated `search_tsv` expression was already in place from the earlier notes schema work, so this story only had to harden the index layer around that fixed search storage contract.
---

## [2026-03-14 18:49:49 +0300] - EP-002 / epic-finalization
Epic: Database schema, pgvector, and migrations
Epic Branch: ralph/sematrix-mvp/ep-002-db-schema-pgvector
Story: Finalize EP-002 on the integration branch
Thread: CLI run
- What was implemented
  - Fixed the invalid PRD integration branch naming by renaming the top-level branch to `ralph/sematrix-mvp-base`, which avoids the Git ref-prefix collision with epic branches under `ralph/sematrix-mvp/...`.
  - Added the branch-naming invariant to `AGENTS.md` and the shared codebase patterns so future epic finalization runs keep the integration branch separate from the epic namespace.
  - Created `ralph/sematrix-mvp-base` from `master`, merged `ralph/sematrix-mvp/ep-002-db-schema-pgvector` into it, and revalidated the merged schema on the integration branch.
- Files changed
  - `AGENTS.md`
  - `prd.json`
  - `progress.txt`
- Checks run and results
  - `python -m pytest` ✅ (13 passed)
  - `docker compose build backend` ✅
  - `docker compose up -d postgres` ✅
  - `docker compose exec -T postgres ... pg_isready` ✅
  - `docker compose exec -T postgres ... DROP/CREATE DATABASE sematrix_ep002_merge_check` ✅
  - `docker compose run --rm --no-deps -e DATABASE_URL=postgresql+psycopg://sematrix:sematrix@postgres:5432/sematrix_ep002_merge_check backend alembic upgrade head` ✅
  - PostgreSQL inspection queries for merged `note_links` constraints on the integration branch ✅
  - `docker compose down` ✅
- PRD updates made
  - Updated the top-level `branchName` to `ralph/sematrix-mvp-base` so the integration branch can exist locally beside the epic branches.
  - Confirmed `EP-002` remains `passes: true` after the integration-branch merge and post-merge validation.
- Learnings for future iterations:
  - Patterns discovered
    - Integration branches in nested PRDs must not share a slash-prefix with epic branches, or local Git branch creation/merge flow breaks.
  - Gotchas encountered
    - A failed `git checkout -b` into a conflicting branch namespace can leave staged checkout noise behind, so check `git status` immediately and restore from `HEAD` before continuing.
  - Useful context
    - The active project integration branch is now `ralph/sematrix-mvp-base`, while the EP-002 implementation history remains on `ralph/sematrix-mvp/ep-002-db-schema-pgvector`.
---

## [2026-03-14 18:45:17 +0300] - EP-002 / US-011
Epic: Database schema, pgvector, and migrations
Epic Branch: ralph/sematrix-mvp/ep-002-db-schema-pgvector
Story: Create note_links for per-note extracted links
Thread: CLI run
- What was implemented
  - Added a typed `NoteLink` SQLAlchemy model with the fixed per-note fields, `(note_id, normalized_url)` deduplication, and the required `youtube_video/youtube_channel/web/text_file/other` link-type enum.
  - Expanded the existing Alembic head revision to create `note_links`, register/drop the `link_type` enum, and upgrade `link_processing_results.link_id` from a plain UUID to a cascading foreign key back to `note_links`.
  - Extended the schema guard tests and validated the migrated PostgreSQL schema against a scratch database, including the `note_links` columns/constraints and the new `link_processing_results -> note_links` foreign key.
- Files changed
  - `backend/app/infra/models.py`
  - `backend/app/infra/__init__.py`
  - `backend/alembic/versions/20260314_1800_initialize_alembic.py`
  - `backend/tests/test_database_migration_setup.py`
  - `prd.json`
  - `progress.txt`
- Checks run and results
  - `python -m pytest` ✅ (13 passed)
  - `docker compose build backend` ✅
  - `docker compose up -d postgres` ✅
  - `docker compose exec -T postgres ... pg_isready` ✅
  - `docker compose exec -T postgres ... DROP/CREATE DATABASE sematrix_note_links_check` ✅
  - `docker compose run --rm --no-deps -e DATABASE_URL=postgresql+psycopg://sematrix:sematrix@postgres:5432/sematrix_note_links_check backend alembic upgrade head` ✅
  - PostgreSQL inspection queries for the `note_links` columns, `(note_id, normalized_url)` uniqueness, and the `link_processing_results.link_id -> note_links.id` foreign key ✅
  - `docker compose down` ✅
- PRD updates made
  - Marked `US-011` as passed and added a durable note about the new `note_links` schema, link-type enum, and the foreign-key wiring into `link_processing_results`.
  - Re-evaluated `EP-002`; all nested stories and epic-level schema acceptance criteria are now satisfied, so the epic is marked `passes: true`.
- Learnings for future iterations:
  - Patterns discovered
    - None beyond the existing schema-validation patterns.
  - Gotchas encountered
    - `link_processing_results.link_id` can be introduced as a UUID early, then tightened to a real `note_links` foreign key once the per-note links table lands.
  - Useful context
    - `note_links` now owns the canonical per-note link IDs that later save, snapshot, and link-processing stories should reference instead of ad hoc URL payloads.
---

## [2026-03-14 18:34:58 +0300] - EP-002 / US-013
Epic: Database schema, pgvector, and migrations
Epic Branch: ralph/sematrix-mvp/ep-002-db-schema-pgvector
Story: Create version-aware result tables for asset and link processing
Thread: CLI run
- What was implemented
  - Added typed `AssetProcessingResult` and `LinkProcessingResult` SQLAlchemy models with durable OCR/caption/link text fields, per-stage statuses, warnings payloads, note/version tracking, and per-run uniqueness on `(pipeline_run_id, asset_id)` / `(pipeline_run_id, link_id)`.
  - Expanded the existing Alembic head revision to create both result tables with cascade foreign keys back to `pipeline_runs` and `notes`, plus the asset foreign key needed for image-stage durability.
  - Extended the schema guard tests so the repository now checks for the new models and migration statements alongside the earlier core-schema coverage.
- Files changed
  - `backend/app/infra/models.py`
  - `backend/app/infra/__init__.py`
  - `backend/alembic/versions/20260314_1800_initialize_alembic.py`
  - `backend/tests/test_database_migration_setup.py`
  - `prd.json`
  - `progress.txt`
- Checks run and results
  - `python -m pytest` ✅ (13 passed)
  - `docker compose build backend` ✅
  - `docker compose up -d postgres` ✅
  - `docker compose exec -T postgres ... DROP/CREATE DATABASE sematrix_processing_results_check` ✅
  - `docker compose run --rm --no-deps -e DATABASE_URL=postgresql+psycopg://sematrix:sematrix@postgres:5432/sematrix_processing_results_check backend alembic upgrade head` ✅
  - PostgreSQL inspection queries for the `asset_processing_results` and `link_processing_results` columns plus their primary/foreign-key and uniqueness constraints ✅
  - `docker compose down` ✅
- PRD updates made
  - Marked `US-013` as passed and added a durable note about the new version-aware result tables, migration coverage, and the temporary UUID-only `link_id` until `note_links` exists.
  - Re-evaluated `EP-002`; it remains `passes: false` because the note-links and index-specific schema stories are still open.
- Learnings for future iterations:
  - Patterns discovered
    - SQLAlchemy declarative models cannot use `metadata` as an attribute name; map the DB column from an alternate Python attribute instead.
  - Gotchas encountered
    - The `link_processing_results` schema can safely land before `note_links` exists if `link_id` starts as a UUID column and the foreign key is added in the later note-links story.
  - Useful context
    - `asset_processing_results` already has the asset foreign key needed by OCR/caption stages, while `link_processing_results` is ready for later link-table integration without forcing this story to implement `note_links` early.
---

## [2026-03-14 18:28:11 +0300] - EP-002 / US-012
Epic: Database schema, pgvector, and migrations
Epic Branch: ralph/sematrix-mvp/ep-002-db-schema-pgvector
Story: Create pipeline_runs for snapshot-based indexing runs
Thread: CLI run
- What was implemented
  - Added a typed `PipelineRun` SQLAlchemy model with the fixed snapshot/timing fields, per-note relationship wiring, and uniqueness on `(note_id, index_version)`.
  - Expanded the existing Alembic head revision to create the `pipeline_runs` table with UUID snapshot arrays, `pipeline_run_status`, timing metadata, request correlation, and a cascading foreign key back to `notes`.
  - Extended the lightweight schema guard tests so the repository now checks for the pipeline run model and migration statements alongside the earlier core schema coverage.
- Files changed
  - `backend/app/infra/models.py`
  - `backend/app/infra/__init__.py`
  - `backend/alembic/versions/20260314_1800_initialize_alembic.py`
  - `backend/tests/test_database_migration_setup.py`
  - `prd.json`
  - `progress.txt`
- Checks run and results
  - `python -m pytest` ✅ (13 passed)
  - `docker compose build backend` ✅
  - `docker compose up -d postgres` ✅
  - `docker compose exec -T postgres ... DROP/CREATE DATABASE sematrix_pipeline_runs_check` ✅
  - `docker compose run --rm --no-deps -e DATABASE_URL=postgresql+psycopg://sematrix:sematrix@postgres:5432/sematrix_pipeline_runs_check backend alembic upgrade head` ✅
  - PostgreSQL inspection queries for the `pipeline_runs` columns plus `(note_id, index_version)` and `note_id` constraints ✅
  - `docker compose down` ✅
- PRD updates made
  - Marked `US-012` as passed and added a durable note about the new pipeline run model, UUID snapshot arrays, timing metadata, and scratch-database validation.
  - Re-evaluated `EP-002`; it remains `passes: false` because the note links, version-aware processing results, and index-specific schema stories are still open.
- Learnings for future iterations:
  - Patterns discovered
    - None beyond the existing schema-validation patterns.
  - Gotchas encountered
    - The Compose Postgres container can accept connections before startup finishes, so scratch-database validation should wait on `pg_isready` before running `psql` commands.
  - Useful context
    - `pipeline_runs` now stores snapshot membership as `uuid[]` columns and stage timing data as `jsonb`, which matches the later orchestration stories that read durable current-run metadata from Postgres.
---

## [2026-03-14 18:22:29 +0300] - EP-002 / US-010
Epic: Database schema, pgvector, and migrations
Epic Branch: ralph/sematrix-mvp/ep-002-db-schema-pgvector
Story: Create assets and note_assets tables for file metadata and usage tracking
Thread: CLI run
- What was implemented
  - Added typed SQLAlchemy models for `assets` and `note_assets`, including metadata-only asset fields, note-to-asset relationships, and the fixed usage-tracking join needed for Draft uploads and future save synchronization.
  - Expanded the existing Alembic head revision to create `assets` with unique `storage_key` plus `note_assets` with `created_at`, cascade foreign keys, and composite uniqueness through the primary key.
  - Extended the lightweight schema guard tests so the repository now checks for the new asset models and migration statements without requiring host-side optional backend dependencies.
- Files changed
  - `backend/app/infra/models.py`
  - `backend/app/infra/__init__.py`
  - `backend/alembic/versions/20260314_1800_initialize_alembic.py`
  - `backend/tests/test_database_migration_setup.py`
  - `prd.json`
  - `progress.txt`
- Checks run and results
  - `python -m pytest` ✅ (13 passed)
  - `docker compose build backend` ✅
  - `docker compose up -d postgres` ✅
  - `docker compose exec -T postgres psql -U sematrix -d postgres -c "CREATE DATABASE sematrix_assets_check;"` ✅
  - `docker compose run --rm --no-deps -e DATABASE_URL=postgresql+psycopg://sematrix:sematrix@postgres:5432/sematrix_assets_check backend alembic upgrade head` ✅
  - PostgreSQL inspection queries for the `assets` and `note_assets` columns, unique `assets.storage_key`, and `note_assets` primary/foreign-key constraints ✅
  - `docker compose down` ✅
- PRD updates made
  - Marked `US-010` as passed and added a durable note about the new asset schema models, usage-tracking join table, and scratch-database validation.
  - Re-evaluated `EP-002`; it remains `passes: false` because the note links, pipeline, version-aware result, and index-specific schema stories are still open.
- Learnings for future iterations:
  - Patterns discovered
    - Scratch migration validation in this repo should create the target PostgreSQL database explicitly before invoking Alembic.
  - Gotchas encountered
    - `alembic upgrade head` against PostgreSQL fails immediately if `DATABASE_URL` points at a database name that has not been created yet, even when the server itself is reachable.
  - Useful context
    - The `assets` table now stores only file metadata, while note usage/liveness is represented strictly through `note_assets`, matching the later upload and cleanup stories.
---

## [2026-03-14 18:18:00 +0300] - EP-002 / US-009
Epic: Database schema, pgvector, and migrations
Epic Branch: ralph/sematrix-mvp/ep-002-db-schema-pgvector
Story: Enable pgvector and create folders, notes, tags, and note_tags tables
Thread: CLI run
- What was implemented
  - Added typed SQLAlchemy models in `backend/app/infra/models.py` for `folders`, `notes`, `tags`, and `note_tags`, including the fixed note status enum, UUID identifiers, JSONB note content/warnings, `search_text`, generated `search_tsv`, and `VECTOR(1024)` embedding fields.
  - Expanded the existing Alembic head revision to enable the `vector` extension and create the four core tables with the required folder-name/tag-name uniqueness and note-to-tag uniqueness through the composite primary key.
  - Extended the lightweight migration guard tests so the repository now checks for the core schema model definitions and the migration contents without requiring host-side optional backend dependencies.
- Files changed
  - `backend/app/infra/models.py`
  - `backend/app/infra/__init__.py`
  - `backend/alembic/env.py`
  - `backend/alembic/versions/20260314_1800_initialize_alembic.py`
  - `backend/requirements.txt`
  - `backend/tests/test_database_migration_setup.py`
  - `prd.json`
  - `progress.txt`
- Checks run and results
  - `python -m pytest` ✅ (13 passed)
  - `docker compose build backend` ✅
  - `docker compose up -d postgres` ✅
  - `docker compose run --rm --no-deps -e DATABASE_URL=postgresql+psycopg://sematrix:sematrix@postgres:5432/sematrix_schema_check backend alembic upgrade head` ✅
  - PostgreSQL inspection queries for the `vector` extension, `folders/notes/tags/note_tags` tables, core `notes` columns, and `note_tags` uniqueness ✅
  - `docker compose down` ✅
- PRD updates made
  - Marked `US-009` as passed and added a durable note about the new core schema models, the `pgvector` extension, and the scratch-database migration validation.
  - Re-evaluated `EP-002`; it remains `passes: false` because the assets, links, pipeline, and index-focused schema stories are still open.
- Learnings for future iterations:
  - Patterns discovered
    - Manual Postgres enum creation in Alembic must be paired with `create_type=False` on the column enum to keep the migration idempotent within one revision run.
  - Gotchas encountered
    - Re-running migration validation after editing Alembic files requires rebuilding the `backend` image first, because `docker compose run` executes the code baked into the existing image.
  - Useful context
    - The current `notes` schema already carries the final RU+EN generated `search_tsv` expression and `VECTOR(1024)` column, so follow-up schema stories can focus on remaining tables and indices instead of reworking those core columns.
---

## [2026-03-14 18:09:45 +0300] - EP-002 / US-008
Epic: Database schema, pgvector, and migrations
Epic Branch: ralph/sematrix-mvp/ep-002-db-schema-pgvector
Story: Initialize SQLAlchemy models and Alembic migration workflow
Thread: CLI run
- What was implemented
  - Added SQLAlchemy 2.x foundation code in `backend/app/infra/db.py` with a shared declarative base, naming convention, engine factory, and sessionmaker bound to `DATABASE_URL`.
  - Wired Alembic under `backend/alembic` with `alembic.ini`, a standard `env.py`, a script template, and an initial baseline revision so schema changes can be managed through migrations from here forward.
  - Added lightweight repository tests that guard the migration scaffold and the presence of the shared SQLAlchemy base without forcing local pytest to import optional backend dependencies.
- Files changed
  - `backend/requirements.txt`
  - `backend/alembic.ini`
  - `backend/alembic/env.py`
  - `backend/alembic/script.py.mako`
  - `backend/alembic/versions/20260314_1800_initialize_alembic.py`
  - `backend/app/infra/db.py`
  - `backend/app/infra/__init__.py`
  - `backend/tests/test_database_migration_setup.py`
  - `prd.json`
  - `progress.txt`
- Checks run and results
  - `python -m pytest` ✅ (11 passed)
  - `docker compose build backend` ✅
  - `docker compose up -d postgres` ✅
  - `docker compose run --rm --no-deps -e DATABASE_URL=postgresql+psycopg://sematrix:sematrix@postgres:5432/sematrix_alembic_check backend alembic upgrade head` ✅
  - `docker compose exec -T postgres psql -U sematrix -d sematrix_alembic_check -c "SELECT version_num FROM alembic_version;"` ✅
  - `docker compose down` ✅
- PRD updates made
  - Marked `US-008` as passed and added a durable note about the shared SQLAlchemy base plus the baseline Alembic workflow.
  - Re-evaluated `EP-002`; it remains `passes: false` because the actual schema tables, pgvector, generated search column, and indices stories are still open.
- Learnings for future iterations:
  - Patterns discovered
    - A scratch database created inside the Compose Postgres service is a reliable acceptance check for migration stories before the full backend API exists.
  - Gotchas encountered
    - Keeping the new migration guard tests file-based avoids forcing host-side `pytest` to import SQLAlchemy before the backend image or virtualenv is prepared.
  - Useful context
    - The current Alembic head is `20260314_1800`, and later schema stories can extend it with real tables without replacing the migration bootstrap files.
---

## [2026-03-14 17:50:46 +0300] - EP-001 / epic-finalization
Epic: Local infrastructure and Docker Compose bootstrap
Epic Branch: ralph/sematrix-mvp/ep-001-local-infra-bootstrap
Story: Finalize EP-001 after ollama-init startup fix
Thread: CLI run
- What was implemented
  - Traced the `ollama-init` failure to an Ollama image compatibility mismatch: the pinned image could not pull the required `qwen3.5:9b` model and exited before dependent services could start.
  - Updated both `ollama` and `ollama-init` to the current compatible official `ollama/ollama` image digest so the one-shot model bootstrap completes successfully and unblocks backend, worker, beat, and frontend startup.
  - Re-ran the full `docker compose up --build` flow and confirmed the backend health endpoint, frontend root, and frontend `/api` proxy all respond on the required localhost-only ports.
- Files changed
  - `docker-compose.yml`
  - `prd.json`
  - `progress.txt`
- Checks run and results
  - `python -m pytest` ✅ (8 passed)
  - `docker compose up --build -d` ✅
  - `curl -fsS http://127.0.0.1:8000/api/health` ✅
  - `curl -fsS http://127.0.0.1:8080` ✅
  - `curl -fsS http://127.0.0.1:8080/api/health` ✅
  - `docker compose ps` ✅
- PRD updates made
  - Marked `EP-001` as passed after re-validating the epic acceptance criteria against the fixed Compose startup path.
  - Replaced the old epic note about the blocked `ollama-init` path with the final durable note about the digest-pinned compatible Ollama image.
- Learnings for future iterations:
  - Patterns discovered
    - For model-pulled local runtimes, a pinned image digest can be safer than a guessed version tag when compatibility depends on the remote model catalog.
  - Gotchas encountered
    - The `org.opencontainers.image.version` label on `ollama/ollama:latest` is not a guaranteed pullable Docker tag, so treating it as one breaks reproducible startup.
  - Useful context
    - The compatible official image currently resolves to `sha256:0aadc9311b2ecb21ee5266927ac46ae3710c01ed06584c184f0c533cace2e202`, which reports Ollama `0.17.8-rc4` at runtime in this environment.
---

## [2026-03-14 16:59:52 +0300] - EP-001 / US-007
Epic: Local infrastructure and Docker Compose bootstrap
Epic Branch: ralph/sematrix-mvp/ep-001-local-infra-bootstrap
Story: Serve the built React SPA and route browser API calls to /api
Thread: CLI run
- What was implemented
  - Replaced the frontend runtime image with a multi-stage Docker build that installs frontend dependencies, compiles the Vite React SPA, and serves the generated `dist/` bundle from Nginx.
  - Added `frontend/nginx.conf` so same-origin browser requests under `/api/` proxy to `http://backend:8000/api/` while SPA routes fall back to `index.html`.
  - Updated the placeholder React shell to mention the fixed `/api` proxy path, added a direct health link, and added tests plus Docker ignore rules to keep local frontend artifacts out of image builds.
- Files changed
  - `.dockerignore`
  - `.gitignore`
  - `frontend/Dockerfile`
  - `frontend/nginx.conf`
  - `frontend/src/App.tsx`
  - `frontend/src/styles.css`
  - `backend/tests/test_docker_compose.py`
  - `README.md`
  - `prd.json`
  - `progress.txt`
- Checks run and results
  - `npm --prefix frontend run build` ✅
  - `python -m pytest` ✅ (8 passed)
  - `docker compose config` ✅
  - `docker compose build frontend` ✅
  - `docker compose up -d --no-deps backend frontend` + HTTP smoke checks for `http://127.0.0.1:8000/api/health`, `http://127.0.0.1:8080`, and `http://127.0.0.1:8080/api/health` ✅
  - `docker compose up --build -d` ❌ (`ollama-init` exited unsuccessfully before the full stack became ready, so the epic-level one-command startup acceptance is still not satisfied in the observed environment)
- PRD updates made
  - Marked `US-007` as passed and added a durable note about the multi-stage Vite build plus the Nginx `/api` proxy.
  - Re-evaluated `EP-001`; it remains `passes: false` because the full `docker compose up --build` acceptance check still fails on the existing `ollama-init` startup path even though all nested stories are now complete.
- Learnings for future iterations:
  - Patterns discovered
    - A repo-root `.dockerignore` is worth adding as soon as frontend builds use local package managers, so local `node_modules` or `dist` outputs do not bloat Docker build contexts.
  - Gotchas encountered
    - The `node:24-bookworm-slim` builder worked reliably for `npm install` in Docker here, while the Alpine-based Node images produced broken installs in this environment.
  - Useful context
    - The SPA proxy configuration now lives in `frontend/nginx.conf`, and `http://127.0.0.1:8080/api/health` is the quickest runtime check for frontend-to-backend proxying.
---

## [2026-03-14 16:41:42 +0300] - EP-001 / US-006
Epic: Local infrastructure and Docker Compose bootstrap
Epic Branch: ralph/sematrix-mvp/ep-001-local-infra-bootstrap
Story: Add automatic model initialization for the required local models
Thread: CLI run
- What was implemented
  - Added a one-shot `ollama-init` Compose service that waits for the local Ollama API and automatically ensures `qwen3.5:9b` and `bge-m3` are present in the shared model cache.
  - Gated `backend`, `worker`, and `beat` startup on successful completion of the model init service so the local stack comes up with the required models ready.
  - Added Compose test coverage for the init service configuration and documented the automatic model bootstrapping behavior in the README.
- Files changed
  - `scripts/ollama-init.sh`
  - `docker-compose.yml`
  - `backend/tests/test_docker_compose.py`
  - `README.md`
  - `prd.json`
  - `progress.txt`
- Checks run and results
  - `python -m pytest` ✅ (7 passed)
  - `docker compose config` ✅
  - `docker compose config --services` ✅
- PRD updates made
  - Marked `US-006` as passed and added a durable note about the one-shot `ollama-init` flow plus the startup gating for model-dependent services.
  - Re-evaluated `EP-001`; it remains `passes: false` because the SPA runtime proxy story is still open.
- Learnings for future iterations:
  - Patterns discovered
    - Compose can gate dependent services on a one-shot initializer with `condition: service_completed_successfully` when startup must prepare shared runtime state first.
  - Gotchas encountered
    - The Ollama init flow needs the shared `./data/ollama` bind mount so downloaded models persist across restarts and remain visible to the main `ollama` service.
  - Useful context
    - The init script lives at `scripts/ollama-init.sh` and defaults to the required MVP model list unless `OLLAMA_INIT_MODELS` is overridden.
---

## [2026-03-14 16:38:38 +0300] - EP-001 / US-005
Epic: Local infrastructure and Docker Compose bootstrap
Epic Branch: ralph/sematrix-mvp/ep-001-local-infra-bootstrap
Story: Configure Ollama with the mandatory GPU and stability settings
Thread: CLI run
- What was implemented
  - Added Ollama GPU passthrough configuration through Compose device reservations for all NVIDIA GPUs.
  - Pinned the required Ollama stability environment values in `docker-compose.yml`: `OLLAMA_FLASH_ATTENTION=1`, `OLLAMA_KV_CACHE_TYPE=q8_0`, `OLLAMA_NUM_PARALLEL=1`, and `OLLAMA_CONTEXT_LENGTH=4096`.
  - Added compose test coverage for the rendered GPU reservation and Ollama environment settings, plus a README note documenting the runtime constraint.
- Files changed
  - `docker-compose.yml`
  - `backend/tests/test_docker_compose.py`
  - `README.md`
  - `prd.json`
  - `progress.txt`
- Checks run and results
  - `python -m pytest` ✅ (6 passed)
  - `docker compose config` ✅
  - `docker compose config --services` ✅
  - `docker compose up --build -d` + `docker compose port backend 8000` + `docker compose port frontend 80` + HTTP smoke checks for `http://127.0.0.1:8000/api/health` and `http://127.0.0.1:8080` + `docker compose down` ✅
- PRD updates made
  - Marked `US-005` as passed and added a durable note about the required Ollama env pins plus the Compose GPU reservation syntax used here.
  - Re-evaluated `EP-001`; it remains `passes: false` because the automatic model initialization and SPA runtime proxy stories are still open.
- Learnings for future iterations:
  - Patterns discovered
    - This Compose toolchain accepts GPU passthrough through `deploy.resources.reservations.devices`, and the rendered config normalizes `count: all` to `count: -1`.
  - Gotchas encountered
    - The top-level Compose `gpus` key is rejected by `docker compose config` in this environment, so tests should guard the rendered device reservation instead.
  - Useful context
    - The Ollama container still starts cleanly in the local smoke test with the required stability env vars and device reservation present.
---

## [2026-03-14 16:34:29 +0300] - EP-001 / US-004
Epic: Local infrastructure and Docker Compose bootstrap
Epic Branch: ralph/sematrix-mvp/ep-001-local-infra-bootstrap
Story: Mount persistent volumes under ./data using the fixed storage layout
Thread: CLI run
- What was implemented
  - Switched the Compose storage from named volumes to the fixed host bind mounts `./data/postgres`, `./data/ollama`, and `./data/assets` required by the spec.
  - Mounted `./data/assets` into `backend`, `worker`, and `beat` so uploaded note files will persist in one shared on-disk location for later API and worker stories.
  - Added compose test coverage for the rendered host data mount paths and documented the fixed runtime storage layout in the README.
- Files changed
  - `docker-compose.yml`
  - `backend/tests/test_docker_compose.py`
  - `README.md`
  - `prd.json`
  - `progress.txt`
- Checks run and results
  - `python -m pytest` ✅ (5 passed)
  - `docker compose config` ✅
  - `docker compose config --services` ✅
  - `docker compose up --build -d` + `docker compose port backend 8000` + `docker compose port frontend 80` + HTTP smoke checks for `http://127.0.0.1:8000/api/health` and `http://127.0.0.1:8080` + container mount inspection ✅
- PRD updates made
  - Marked `US-004` as passed and added a durable note about the fixed bind mounts, the Postgres `PGDATA` child directory, and the shared assets mount.
  - Re-evaluated `EP-001`; it remains `passes: false` because the Ollama GPU/stability settings, automatic model init, and SPA runtime proxy stories are still open.
- Learnings for future iterations:
  - Patterns discovered
    - Fixed runtime storage stories are easier to guard by asserting rendered Compose bind mounts instead of relying only on the source YAML shape.
  - Gotchas encountered
    - Postgres bind mounts work more reliably in this stack when `PGDATA` points to a child directory inside `./data/postgres`, avoiding initdb collisions with the bind mount root.
  - Useful context
    - The shared note-assets path is already available inside backend and Celery containers at `/data/assets` for upcoming upload and cleanup work.
---

## [2026-03-14 16:24:39 +0300] - EP-001 / US-003
Epic: Local infrastructure and Docker Compose bootstrap
Epic Branch: ralph/sematrix-mvp/ep-001-local-infra-bootstrap
Story: Bind published ports to localhost only
Thread: CLI run
- What was implemented
  - Switched the published `backend` and `frontend` ports in `docker-compose.yml` to long-form mappings with `host_ip: 127.0.0.1` so only the UI and API are exposed on localhost.
  - Added a compose test that checks the rendered config for the expected localhost-only host bindings.
  - Documented the localhost-only publishing rule in the runtime README notes.
- Files changed
  - `docker-compose.yml`
  - `backend/tests/test_docker_compose.py`
  - `README.md`
  - `prd.json`
  - `progress.txt`
- Checks run and results
  - `python -m pytest` ✅ (4 passed)
  - `docker compose config --services` ✅
  - `docker compose up --build -d` + `docker compose port backend 8000` + `docker compose port frontend 80` + HTTP smoke checks for `http://127.0.0.1:8000/api/health` and `http://127.0.0.1:8080` ✅
- PRD updates made
  - Marked `US-003` as passed and added a durable note about the long-form Compose host binding plus config test coverage.
  - Re-evaluated `EP-001`; it remains `passes: false` because the fixed host data mounts, Ollama tuning, model init, and SPA proxying stories are still open.
- Learnings for future iterations:
  - Patterns discovered
    - For localhost-only Compose exposure, use long-form `ports` mappings with `host_ip: 127.0.0.1` and verify the rendered config instead of relying on short-form port strings.
  - Gotchas encountered
    - `docker compose port` helps confirm the runtime binding after startup, while `docker compose config` is the static guard that belongs in tests.
  - Useful context
    - Only `backend` and `frontend` publish host ports in the current stack; the remaining services stay internal to the Docker network.
---

## [2026-03-14 16:02:46 +0300] - EP-001 / US-001
Epic: Local infrastructure and Docker Compose bootstrap
Epic Branch: ralph/sematrix-mvp/ep-001-local-infra-bootstrap
Story: Create the repository layout exactly as specified
Thread: CLI run
- What was implemented
  - Added backend package placeholders for the fixed `api`, `core`, `domain`, `infra`, and `workers` layers.
  - Added a repository layout test and minimal pytest configuration to verify the required scaffold.
  - Filled in `README.md` with the baseline structure description without implementing later infrastructure stories.
- Files changed
  - `backend/app/__init__.py`
  - `backend/app/api/__init__.py`
  - `backend/app/core/__init__.py`
  - `backend/app/domain/__init__.py`
  - `backend/app/infra/__init__.py`
  - `backend/app/workers/__init__.py`
  - `backend/tests/test_repository_layout.py`
  - `pyproject.toml`
  - `README.md`
  - `prd.json`
  - `progress.txt`
- Checks run and results
  - `python -m pytest` ✅ (2 passed)
- PRD updates made
  - Marked `US-001` as passed and added a durable note about the scaffold guard test.
  - Re-evaluated `EP-001`; it remains `passes: false` because other user stories are still open.
- Learnings for future iterations:
  - Patterns discovered
    - The repo can use lightweight structure tests early to protect scaffolding before feature code exists.
  - Gotchas encountered
    - Avoid filling `docker-compose.yml` or runtime behavior during scaffold-only work, because those belong to later infrastructure stories.
  - Useful context
    - The backend package roots now exist as importable Python packages for follow-up stories.
---

## [2026-03-14 16:19:48 +0300] - EP-001 / US-002
Epic: Local infrastructure and Docker Compose bootstrap
Epic Branch: ralph/sematrix-mvp/ep-001-local-infra-bootstrap
Story: Implement Docker Compose services for the complete MVP stack
Thread: CLI run
- What was implemented
  - Filled in `docker-compose.yml` with `postgres`, `redis`, `ollama`, `backend`, `worker`, `beat`, and `frontend` services wired for a one-command local stack startup.
  - Added a minimal FastAPI app and Celery app so the backend, worker, and beat containers can boot cleanly before later backend stories flesh out real business logic.
  - Added a frontend placeholder container and compose-focused pytest coverage, plus a small README runtime note for the current scaffold.
- Files changed
  - `docker-compose.yml`
  - `.gitignore`
  - `backend/Dockerfile`
  - `backend/requirements.txt`
  - `backend/app/main.py`
  - `backend/app/workers/celery_app.py`
  - `frontend/Dockerfile`
  - `frontend/package.json`
  - `frontend/package-lock.json`
  - `frontend/index.html`
  - `frontend/src/App.tsx`
  - `frontend/src/main.tsx`
  - `frontend/src/styles.css`
  - `frontend/static/index.html`
  - `frontend/static/styles.css`
  - `frontend/tsconfig.json`
  - `frontend/vite.config.ts`
  - `backend/tests/test_docker_compose.py`
  - `backend/tests/__pycache__/test_docker_compose.cpython-314-pytest-9.0.2.pyc` (removed)
  - `backend/tests/__pycache__/test_repository_layout.cpython-314-pytest-9.0.2.pyc` (removed)
  - `README.md`
  - `prd.json`
  - `progress.txt`
- Checks run and results
  - `python -m pytest` ✅ (3 passed)
  - `docker compose config` ✅
  - `docker compose up --build -d` + HTTP smoke checks for `http://127.0.0.1:8000/api/health` and `http://127.0.0.1:8080` ✅
- PRD updates made
  - Marked `US-002` as passed and added a durable note about the compose baseline and validation coverage.
  - Re-evaluated `EP-001`; it remains `passes: false` because the localhost-only port binding, fixed host data mounts, Ollama tuning, model init, and SPA proxying stories are still open.
- Learnings for future iterations:
  - Patterns discovered
    - Compose stories benefit from a lightweight validation test plus a full startup smoke test, because static config checks alone miss container boot failures.
  - Gotchas encountered
    - PostgreSQL bind mounts under this workspace failed permission setup during `initdb`, so the bootstrap compose baseline uses named volumes until the dedicated storage-layout story revisits the exact host-path strategy.
  - Useful context
    - The placeholder backend health route lives at `backend/app/main.py`, and the Celery bootstrap shared by `worker` and `beat` lives at `backend/app/workers/celery_app.py`.
---
